# Graph_agent

Container for the course orchestrator agent for working with kafka

### How to launch:

Clone repository, run docker build and run container with env's values

### env's values:

LANGFUSE_SECRET_KEY, LANGFUSE_PUBLIC_KEY
